import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Disable Turbopack to avoid internal errors
  // Use webpack bundler instead for stability
};

export default nextConfig;
